# Upgrade to v0.6.5

Copy this snapshot over v0.6.4 while preserving `.git` and `.venv`, reinstall editable dependencies, run Ruff and pytest, then commit. No database migration or environment-variable change is required.
